# 项目代理规范

## 仓库用途

- 本仓库保存 [bugwz/ProxyAssistant](https://github.com/bugwz/ProxyAssistant) 项目使用的版本化资源文件。
- `assets/` 是下一次完整资源快照的暂存目录，不是增量补丁目录。

## 分支与发布

- `main` 不得提交具体资源，只保留空的 `assets/`、仓库配置和发布工具。
- 资源分支必须使用本地时间格式 `yyyyMMddHHmmss` 命名。
- 每个时间分支都是不可变的完整快照；不得复用、修改或强制推送已发布分支。
- 必须保留 `assets/` 内用户提供的目录结构。
- 不得在资源发布过程中修改 `origin` 的地址。
- 当用户要求发布 `assets/` 中的资源时，加载并遵循 `publish` 技能；若当前代理未自动发现该技能，则读取 `.agents/skills/publish/SKILL.md`。
- 优先运行 `./scripts/publish-assets.sh` 完成校验、建分支、提交和推送，不要手工复制一套不同的发布步骤。

## Git 提交

- 当用户要求创建提交或整理提交信息时，加载并遵循 `commit` 技能。
- 未展示完整提交信息和拟提交文件、未获得用户明确确认前，不得执行 `git add` 或 `git commit`。

## 文档格式

- 编辑任何 Markdown 文件（`*.md`）时，不要为了控制行宽主动换行；每个普通段落和列表项各自保持在一个物理行中，仅在标题、空行、代码块、表格或列表层级等 Markdown 结构确实需要时换行。

## 安全边界

- 发布前确认当前分支为 `main`，且待提交变更全部位于 `assets/`。
- 不得丢弃或夹带无关改动。
- 推送失败时保留原时间分支及提交用于重试，不得改写历史或创建重复快照。
